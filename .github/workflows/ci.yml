name: CI

on:
  push:
  pull_request:

jobs:
  sync-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check synced files
        run: ./scripts/sync-files.sh --check
